pre-commit:
  commands:
    lint:
      run: pnpm check:fix

    typecheck:
      run: pnpm check:type

pre-push:
  parallel: false
  commands:
    pull:
      run: git pull origin $(git branch --show-current)

    test:
      run: pnpm test:unit && pnpm test:int && pnpm test:e2e

   